import CardCode from "components/CardCode";
import styles from "./EditorCode.module.css";

const EditorCode = () => {
    return (
        <main className={styles.main}>
            <CardCode/>
        </main>
    )
}

export default EditorCode;